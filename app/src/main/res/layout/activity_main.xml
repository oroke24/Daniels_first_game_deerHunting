<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#444444"
    tools:context=".MainActivity">

    <RelativeLayout
        android:id="@+id/gameLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#44FF22"
        android:gravity="center">

        <Button
            android:id="@+id/version1Button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_marginStart="10dp"
            android:layout_marginTop="10dp"
            android:layout_marginEnd="10dp"
            android:layout_marginBottom="10dp"
            android:background="@drawable/drawable_rounded_corners"
            android:padding="25dp"
            android:text="Classic"
            android:textSize="55sp"
            android:textStyle="bold"
            android:visibility="visible" />

        <Button
            android:id="@+id/timedVersionButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/version1Button"
            android:layout_marginStart="10dp"
            android:layout_marginTop="10dp"
            android:layout_marginEnd="10dp"
            android:layout_marginBottom="10dp"
            android:background="@drawable/drawable_rounded_corners"
            android:padding="25dp"
            android:text="Timed Version"
            android:textSize="35sp"
            android:textStyle="bold"
            android:visibility="visible" />

    </RelativeLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_alignParentBottom="true"
        android:background="#333333"
        android:orientation="vertical">

        <!-- ListView for records -->
        <ListView
            android:id="@+id/recordListView"
            android:layout_width="match_parent"
            android:layout_height="500dp"
            android:divider="@android:color/transparent"
            android:dividerHeight="16dp" />

        <!-- Add any other views or features here -->
    </LinearLayout>

</RelativeLayout>